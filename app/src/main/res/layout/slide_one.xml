<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="@drawable/bg_onboard_gradient">

    <!-- Illustation -->
    <ImageView
        android:id="@+id/imgIllustration1"
        android:layout_width="260dp"
        android:layout_height="260dp"
        android:src="@drawable/ic_onboard_welcome"
        android:layout_marginTop="32dp"
        android:contentDescription="Welcome illustration"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- Title -->
    <TextView
        android:id="@+id/tvTitle1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Welcome to FitLife"
        android:textSize="28sp"
        android:textStyle="bold"
        android:textColor="@color/navy_primary"
        app:layout_constraintTop_toBottomOf="@id/imgIllustration1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="26dp"/>

    <!-- Subtitle -->
    <TextView
        android:id="@+id/tvSubtitle1"
        android:layout_width="280dp"
        android:layout_height="wrap_content"
        android:text="Mulai perjalanan hidup sehatmu dengan rekomendasi yang dipersonalisasi."
        android:textSize="16sp"
        android:textColor="@color/text_secondary"
        android:gravity="center"
        app:layout_constraintTop_toBottomOf="@id/tvTitle1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>

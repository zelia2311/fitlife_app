<?xml version="1.0" encoding="utf-8"?>
<GridLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:columnCount="2"
    android:rowCount="2"
    android:padding="16dp"
    tools:context=".MealCaloriesFragment">

    <include layout="@layout/item_progress_ring"
        android:id="@+id/carbs_progress_ring" />

    <include layout="@layout/item_progress_ring"
        android:id="@+id/protein_progress_ring"/>

    <include layout="@layout/item_progress_ring"
        android:id="@+id/sugar_progress_ring" />

    <include layout="@layout/item_progress_ring"
        android:id="@+id/fat_progress_ring" />

</GridLayout>
